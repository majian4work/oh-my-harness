# oh-my-harness

The orchestration framework for ai agents.

## Workspace layout

- `libs/agent`: shared library crate used by applications
- `bins/omh`: terminal UI (`omh`) binary crate

## Run

```bash
cargo run -p omh
```
